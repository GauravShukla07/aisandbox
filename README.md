# aisandbox

Sandbox repo for the internal RAG chatbot prototype (Streamlit +
Bedrock Knowledge Bases + Claude Sonnet via `boto3`).

## What's here

- [`docs/deployment/`](./docs/deployment/) — full deployment plan for
  the demo run, including seven evaluated approaches, an
  authentication chapter, a weighted comparison matrix, and a
  concrete recommendation with build steps.

Start at [`docs/deployment/README.md`](./docs/deployment/README.md).
